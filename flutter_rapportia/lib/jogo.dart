import 'package:flutter/material.dart';
import 'package:flutter_rapportia/main.dart';

void jogopag() {
  runApp(JogoPag());
}

class JogoPag extends StatelessWidget{
  const JogoPag({super.key});

  @override

  Widget build(BuildContext context) {
    return Scaffold();
  }
}